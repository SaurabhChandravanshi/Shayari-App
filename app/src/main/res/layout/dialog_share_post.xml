<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/round_border"
    android:orientation="vertical">

<ImageView
    android:id="@+id/share_post_image"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="-8dp"
    android:minHeight="200dp"/>

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:weightSum="3"
    android:layout_marginTop="15dp"
    android:layout_marginBottom="6dp"
    android:orientation="horizontal">
    <Button
        android:id="@+id/share_post_close_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clickable="true"
        android:drawableTop="@drawable/ic_baseline_close_24"
        android:background="@android:color/transparent"
        android:text="CLOSE"
        android:fontFamily="@font/sen_bold"
        android:textSize="10sp"
        android:focusable="true" />
    <Button
        android:id="@+id/share_post_copy_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:drawableTop="@drawable/ic_copy_24"
        android:background="@android:color/transparent"
        android:text="COPY"
        android:fontFamily="@font/sen_bold"
        android:textSize="10sp"/>
    <Button
        android:id="@+id/share_post_share_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:drawableTop="@drawable/ic_share_24"
        android:background="@android:color/transparent"
        android:text="SHARE"
        android:fontFamily="@font/sen_bold"
        android:textSize="10sp"/>
</LinearLayout>
</LinearLayout>