<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/round_border"
    android:orientation="vertical">
    
<androidx.cardview.widget.CardView
    android:id="@+id/editor_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="200dp"
    app:cardBackgroundColor="@color/colorRed">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="200dp">
        <EditText
            android:id="@+id/editor_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="30dp"
            android:textAlignment="center"
            android:textColor="@android:color/white"
            android:fontFamily="@font/palanquin_dark_regular"
            android:maxLines="15"
            android:inputType="textMultiLine"
            android:lineSpacingExtra="6dp"
            android:background="@android:color/transparent"
            android:layout_centerInParent="true"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:gravity="center_horizontal" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:weightSum="3"
    android:layout_marginTop="15dp"
    android:layout_marginBottom="6dp"
    android:orientation="horizontal">
    <Button
        android:id="@+id/editor_close_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clickable="true"
        android:drawableTop="@drawable/ic_baseline_close_24"
        android:background="@android:color/transparent"
        android:text="Close"
        android:fontFamily="@font/sans_regular"
        android:textSize="10sp"
        android:focusable="true" />
    <Button
        android:id="@+id/editor_bg_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:drawableTop="@drawable/ic_color_palette_black_24"
        android:background="@android:color/transparent"
        android:text="Background"
        android:fontFamily="@font/sans_regular"
        android:textSize="10sp"/>
    <Button
        android:id="@+id/editor_share_btn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:drawableTop="@drawable/ic_share_24"
        android:background="@android:color/transparent"
        android:text="Share"
        android:fontFamily="@font/sans_regular"
        android:textSize="10sp"/>
</LinearLayout>
</LinearLayout>